<%= form_for notification_configuration, remote: true do |f| %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.submit "Generate notification settings", class: "btn btn-primary btn-sm" %>
<%end%>	